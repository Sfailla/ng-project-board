import { IonIconTitleDirective } from './icon-title.directive'

describe('IconTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new IonIconTitleDirective()
    expect(directive).toBeTruthy()
  })
})
