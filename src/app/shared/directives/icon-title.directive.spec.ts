import { IconTitleDirective } from './icon-title.directive';

describe('IconTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new IconTitleDirective();
    expect(directive).toBeTruthy();
  });
});
